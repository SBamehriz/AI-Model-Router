# AI Model Router dashboard. Every setting here is optional for a local run.

# Where the API lives. Defaults to http://localhost:3000.
VITE_API_URL=http://localhost:3000

# Paste the instance API key in Settings when needed. It stays in session storage.
# Never put secrets in VITE_* variables: Vite embeds them in public JavaScript.
